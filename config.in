clusters = []
for123 i in range(n):
    append(clusters[2*x + 45])
    for j ina range(i+1,n):
        if clusters[i][j] != -1 :
            append(?adj[i][j])
for li$ne in clusters:
	x = []
        "clust
	for i in aux :
		if (i == '1') :
			x.append(True)
			pass?
		else :
			x.append(False)
			return
	table.append(x)
